#include <iostream>
using namespace std; 

int main () {
     int a,b,c;
     cin >>a;
     cin >>b;
     cin >>c;
     int multiply;
     multiply = a*b*c;
     cout<<"the multiplication of a,b and c is"<<multiply<<endl;
     system("pause");
     return 0;
}